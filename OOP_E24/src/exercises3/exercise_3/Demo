package session3.Exercise3;
/*Write an interface to represent a shape. The interface should
have methods for computing the total area, the circumference, 
and the number of edges of the shape. Write classes for each of these shapes: 
square, rectangle, triangle, circle, and line. */
public class Demo {

    public static void main(String[] args) {
        
        Square square = new Square();
        System.out.println("Area: " + square.area() + " Perimeter: " + square.circumference() 
        + " Number of edges: " + square.edges());

        Rectangle rectangle = new Rectangle();
        System.out.println("Area: " + rectangle.area() + " Perimeter: " + rectangle.circumference()
        + " Number of edges: " + rectangle.edges());

        Circle circle = new Circle();
        System.out.println("Area: " + circle.area() + " Circumference: " + circle.circumference()
        + " Number of edges: " + circle.edges());

    }

}
