package session3.Exercise2;
    /*Write an interface to represent a color. A color should be represented as an intensity, valued from 0 to 255, of each of the three components:
    red, green, and blue. The interface should have methods to return the values
    of each of these components. Write classes, which implement the interface,
    for each of these colors: red, green, blue, black, white, gray, cyan. */

public interface Colour {

    int getRed();
    int getGreen();
    int getBlue();
     
}
